## 1. Database Schema

- [x] 1.1 Create `academic_tasks` table in Xano
- [x] 1.2 Add `title` field (text) to `academic_tasks` table
- [x] 1.3 Add `description` field (text) to `academic_tasks` table
- [x] 1.4 Add `due_date` field (date) to `academic_tasks` table
- [x] 1.5 Add `status` field (text) to `academic_tasks` table
- [x] 1.6 Add `subject_id` field (table reference to `subjects`) to `academic_tasks` table
- [x] 1.7 Add `user_id` field (table reference to `users`) to `academic_tasks` table
